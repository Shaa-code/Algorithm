```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int[] arr = new int[n];
        for(int i = 0 ; i<n/2+1 ; i++){
            arr[i] = i+1;
        }
        int lt = 0, rt = 0, sum = 0, cnt = 0;
        while(rt != arr.length){
            if(sum < n){
                sum += arr[rt++];
            }else if(sum > n){
                sum -= arr[lt++];
            }else{
                cnt++;
                sum -= arr[lt++];
            }
        }
        System.out.println(cnt);
    }
}
```

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int[] arr = new int[n];
        int m = n / 2 + 1;
        for(int i = 0 ; i< m ; i++){
            arr[i] = i+1;
        }
        int lt = 0, sum = 0, cnt = 0;
        for(int rt = 0; rt < m ; rt++) {
            sum += arr[rt];
            if (sum == n) cnt++;
            while (sum >= n) {
                sum -= arr[lt++];
                if (sum == n) cnt++;
            }
        }
        System.out.println(cnt);
    }
}
```

수학으로 푸는 방법

```java
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Main {
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int n = Integer.parseInt(br.readLine());
        int ret = 0, cnt = 1;
        n--;
        while(n>0){
            cnt++;
            n -= cnt;
            if(n % cnt == 0) ret++;
        }
        System.out.println(ret);
    }
}

```

![Untitled](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/0002a9f3-aef6-46e7-99c3-982252f84efb/Untitled.png)
